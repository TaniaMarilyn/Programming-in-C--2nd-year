#include<stdio.h>
void main()
{
    int a,count=0;
    printf("enter any number:");
    scanf("%d",&a);
    for(int i=2;i<a;i++)
    {
        if(a%1==0)
            count=count+1;
    }
    if(count==0)
        printf("composite");
    else
        printf("prime");
}
