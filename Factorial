#include<stdio.h>
int fact(int);
int fact(int a)
{
    if(a==0)
    {
        return 1;
    }
    else
    {
        return a*fact(a-1);
    }
}
int main()
{
    int a;
    printf("enter any number:");
    scanf("%d",&a);
    printf("factorial of %d is:%d\n",a,fact(a));
    return 0;
}
